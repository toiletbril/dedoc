things that should be tested I think:

ft:
  - fetch --force

ls:
  - ls --non-local
  - ls --all --non-local
  - ls --search
  - ls -nd, ls --porcelain
  - ls -n and combinations

dl:
  - dl <many>
  - dl --update-all, with error and without
  - dl --force, make sure it replaces files

rm:
  - rm <many>
  - rm --purge-all
  - edge cases

op:
  - op --html
  - op --columns
  - op --line-numbers

ss:
  - ss --whole
  - ss --whole --open
  - ss --precise
  - ss with, without and with missing fragment
  - ss --whole --precise
  - ss --open --ignore-fragment
  - ss --open --columns
  - ss --open --line-numbers

